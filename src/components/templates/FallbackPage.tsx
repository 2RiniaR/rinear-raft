import React from "react";
import { Background } from "components/parts";

export const FallbackPage = (): JSX.Element => (
  <Background>
    <div style={{ minHeight: "100vh" }} />
  </Background>
);
