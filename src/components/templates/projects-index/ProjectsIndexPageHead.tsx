import Head from "next/head";
import React from "react";

const ProjectIndexPageHead = (): JSX.Element => (
  <Head>
    <title>Projects - RineaR</title>
  </Head>
);

export default ProjectIndexPageHead;
