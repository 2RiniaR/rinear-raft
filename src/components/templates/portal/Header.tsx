import React from "react";
import styles from "./Header.module.scss";

const header: JSX.Element = (
  <div className={styles.header}>
    <head>
      <link rel="stylesheet" href="https://use.typekit.net/awt3qpl.css" />
      <title />
    </head>
    <h1 className={styles.title}>Rinear</h1>
    <h2 className={styles.description}>the project by Rinia</h2>
  </div>
);

export default header;
